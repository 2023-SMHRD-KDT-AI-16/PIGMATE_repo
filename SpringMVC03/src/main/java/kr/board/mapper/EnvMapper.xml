<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.board.mapper.EnvMapper">

<select id="getEnv" parameterType="String" resultType="kr.board.entity.Env">
	select * from farm_env_info where mem_id = #{mem_id}
</select>

</mapper>