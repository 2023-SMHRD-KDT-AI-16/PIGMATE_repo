<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.board.mapper.MemberMapper">
	
	<select id="login" parameterType="kr.board.entity.Member" resultType="kr.board.entity.Member">
		select * from member_info where mem_id = #{mem_id} and mem_pw = MD5(#{mem_pw})
	</select>
	
	<select id="getMember" parameterType="String" resultType="kr.board.entity.Member">
		select * from member_info where mem_id = #{mem_id}
	</select>
	
	<insert id="join" parameterType="kr.board.entity.Member">
		insert into member_info(mem_id, mem_pw, mem_name, mem_phone, joined_at, mem_email)
		values(#{mem_id}, MD5(#{mem_pw}),#{mem_name}, #{mem_phone}, current_timestamp, #{mem_email})
	</insert>
	
</mapper>







